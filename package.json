{
  "name": "mat-metric-admin-api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@mat-metric/db": "workspace:*",
    "@mat-metric/types": "workspace:*",
    "hono": "^4.11.1"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}